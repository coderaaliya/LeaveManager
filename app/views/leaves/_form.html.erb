<div class="container text-center">
<%= form_with(model: leafe) do |form| %>
  <% if leafe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leafe.errors.count, "error") %> prohibited this leafe from being saved:</h2>

      <ul>
        <% leafe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <%= form.label :status, style: "display: block" %>
  <%= form.select :status, ['pending'], { selected: 'pending' }, id: "status" %>
  </div>

  <div>
    <%= form.label :reason, style: "display: block" %>
    <%= form.text_area :reason %>
  </div>

  <div>
    <%= form.label :from_date, style: "display: block" %>
    <%= form.date_field :from_date %>
  </div>

  <div>
    <%= form.label :to_date, style: "display: block" %>
    <%= form.date_field :to_date %>
  </div>

  <div>
    <%= form.label :leave_type, style: "display: block" %>
    <%= form.select :leave_type, Leafe.leave_types.keys,{},id: "leave_type" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
</div>